import Image from 'next/image';
import {PageHero,PlaceholderImage,ResourceCard} from '@/components/ui';
import {destinations} from '@/lib/data';
export const metadata={title:'Family Travel Guides'};
export default function Travel(){return <><PageHero eyebrow="Field guides" title="Go somewhere. Notice everything." intro="Family-friendly guides to museums, trails, tide pools, national parks, heritage places, and buildings worth looking up for." accent="bg-sky"/><section className="px-5 py-20 md:px-10"><div className="mx-auto max-w-7xl"><div className="grid gap-10">{destinations.map((d,i)=><article key={d.name} className="grid gap-7 border-b border-ink/15 pb-10 md:grid-cols-2 md:items-center">{'image' in d&&d.image?<div className="relative aspect-[16/10] overflow-hidden rounded-[2rem] shadow-field"><Image src={d.image} alt={`Manasi exploring ${d.name}`} fill sizes="(min-width:768px) 50vw, 100vw" className="object-cover"/></div>:<PlaceholderImage className="aspect-[16/10]" tone={d.color} label={`${d.name}, ${d.place}`}/>}<div className={i%2?'md:-order-1':''}><p className="text-xs uppercase tracking-widest text-clay">{d.place}</p><h2 className="mt-3 font-serif text-4xl md:text-5xl">{d.name}</h2><p className="mt-5 max-w-lg leading-relaxed opacity-70">A slow family guide with what to notice, where to pause, practical accessibility notes, architecture stories, and activities for the journey home.</p><div className="mt-6 flex flex-wrap gap-2">{d.tags.map(t=><span key={t} className="rounded-full bg-ink/5 px-3 py-1 text-xs dark:bg-paper/10">{t}</span>)}</div><div className="mt-7 grid grid-cols-3 gap-3 text-xs"><span>◉ Map included</span><span>⌁ Practical tips</span><span>↓ Checklist</span></div></div></article>)}</div><h2 className="mt-24 font-serif text-4xl">Pack a little curiosity</h2><div className="mt-7 grid gap-5 md:grid-cols-3"><ResourceCard title="National Park Day Pack" type="Checklist" meta="2 pages · PDF"/><ResourceCard title="Museum Looking Game" type="Activity" meta="1 page · PDF"/><ResourceCard title="Junior Ranger Tracker" type="Field log" meta="4 pages · PDF"/></div></div></section></>}
