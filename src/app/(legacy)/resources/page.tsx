import {PageHero,ResourceCard,Newsletter} from '@/components/ui';import {resources} from '@/lib/data';
export const metadata={title:'Free Resources'};
export default function Resources(){return <><PageHero eyebrow="The resource shelf" title="Print it. Pack it. Take it outside." intro="Free booklists, observation sheets, travel planners, and gentle prompts designed to make family exploration feel easier." accent="bg-moss"/><section className="px-5 py-20 md:px-10"><div className="mx-auto max-w-7xl"><div className="mb-12 flex flex-wrap gap-2">{['All resources','Architecture','Museums','Travel','Books','Nature'].map((x,i)=><span className={`rounded-full px-4 py-2 text-sm ${i===0?'bg-ink text-paper dark:bg-paper dark:text-ink':'bg-ink/5 dark:bg-paper/10'}`} key={x}>{x}</span>)}</div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{resources.map(r=><ResourceCard key={r.title} {...r}/>)}</div><p className="mt-10 text-center text-sm opacity-60">Downloads are in preparation. Resource cards are previews of the forthcoming library.</p></div></section><Newsletter/></>}
