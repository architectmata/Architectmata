# Architectmata

A responsive editorial website built with Next.js, TypeScript, Tailwind CSS, and Framer Motion.

## Run locally

```bash
pnpm install
pnpm dev
```

Open `http://localhost:3000`.

Sample books, journal posts, destinations, and resources live in `lib/data.ts`. Forms demonstrate the complete interaction and should be connected to an email service before launch.
